# API Base URL
# Leave empty to use relative paths (recommended if using Vite Proxy or serving from wwwroot)
# Set to full URL (e.g., https://localhost:5001) if backend is on a different domain/port without proxy
VITE_API_URL="https://localhost:7139"