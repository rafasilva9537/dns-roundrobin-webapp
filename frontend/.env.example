# API Base URL
# Leave empty to use relative paths (recommended if using Vite Proxy or serving from wwwroot)
# Set to full URL (e.g., https://localhost:5001) if backend is on a different domain/port without
# TODO: update to https when HTTPS is enabled in backend
VITE_API_URL="http://www.meutrabalho.com.br"